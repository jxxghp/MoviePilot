"""组合根装配出的端口实现与它们的类型化运行时形状。

`context.py` 声明形状：请求级仓储/事务工厂协议，以及把它们按领域收拢的冻结运行时；
其余模块是应用层所声明端口在 SQLAlchemy 与 Oper 上的实现。两类合在一处，因为它们
都不是「在某个时刻执行一次的动作」，而是组合根构造出来、交给别处长期持有的对象。

本目录是宿主里唯一允许同时依赖 `app/db` 与 `app/application` 的地方：端口的声明方在
application，落地方在 db，只有组合根两边都看得见。
"""
