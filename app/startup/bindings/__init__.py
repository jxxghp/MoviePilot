"""组合根向消费方公开的绑定表。

本包的成员把命令词、作业标识、数据库方言这类抽象坐标，绑定到具体的业务链与
基础设施实现。绑定认识全部业务域，消费它的引擎一个也不认识；绑定因此归组合根
持有，而不是归引擎。

与顶层 ``*_initializer.py`` 的分界是「调用方拿它做什么」：初始化动作在组合根
指定的时刻被执行一次，执行完不再被读取；绑定表由消费方自己决定何时读、可重复
读，读到的条目由消费方在更晚的时刻执行。判据见 ``docs/rules/05-architecture.md``
的判据 S。

一族绑定占一个模块；需要多于一个模块时占一个子包。
"""
