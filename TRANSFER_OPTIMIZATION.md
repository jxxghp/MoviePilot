# MoviePilot 文件传输优化

## 问题描述

在 MoviePilot v2.7.6 版本中，当通过外部挂载的 cd2 整理 115 文件时，系统会先完整下载文件再重新上传，而不是直接进行秒传。这是因为新增的整理队列实时进度显示和手动中止功能导致的。

## 问题原因

1. **新增的进度显示功能**：v2.7.6 版本新增了整理队列的实时进度显示和手动中止功能
2. **进度回调机制**：为了实现进度显示，系统在文件传输过程中使用了 `transfer_process` 函数
3. **传输方式变化**：为了支持进度显示和中止功能，系统在处理跨存储传输时会先下载到本地临时目录，然后再上传到目标位置

## 解决方案

### 方案1：优化传输逻辑

修改了 `app/modules/filemanager/transhandler.py` 中的 `__transfer_command` 方法：

- 优先处理同类型网盘之间的传输，避免先下载再上传
- 当源存储和目标存储都是 115 时，直接使用 115 的 move/copy API

### 方案2：添加配置选项

在 `app/core/config.py` 中添加了新的配置选项：

```python
# 是否启用文件传输进度显示（禁用可避免跨存储传输时的重复下载上传）
ENABLE_TRANSFER_PROGRESS: bool = True
```

### 方案3：优化进度显示

修改了 `app/modules/filemanager/storages/__init__.py` 中的 `transfer_process` 函数：

- 根据配置决定是否启用进度显示
- 禁用进度显示时返回空函数，避免不必要的进度更新

修改了 `app/modules/filemanager/storages/u115.py` 中的 upload 和 download 方法：

- 根据配置决定是否启用中止检查
- 禁用进度显示时跳过中止检查

## 使用方法

### 方法1：禁用进度显示（推荐）

在 MoviePilot 的系统设置中，将 `ENABLE_TRANSFER_PROGRESS` 设置为 `False`，这样可以：

- 避免跨存储传输时的重复下载上传
- 提高传输效率
- 减少网络带宽使用

### 方法2：保持进度显示

如果用户需要进度显示功能，可以保持 `ENABLE_TRANSFER_PROGRESS` 为 `True`，系统会自动优化同类型网盘之间的传输。

## 影响范围

这些修改主要影响：

1. **115 网盘之间的文件传输**：现在会直接使用 115 的 API，避免先下载再上传
2. **其他网盘之间的传输**：同样会优先使用网盘的原生 API
3. **进度显示功能**：可以通过配置选择是否启用

## 兼容性

- 向后兼容：所有修改都保持了向后兼容性
- 配置可选：用户可以选择是否启用进度显示
- 功能完整：所有原有功能都得到保留

## 测试建议

1. 测试 115 网盘之间的文件整理
2. 测试跨存储的文件整理
3. 测试进度显示功能的开关
4. 测试手动中止功能

## 注意事项

1. 禁用进度显示后，用户将无法看到文件传输的实时进度
2. 禁用进度显示后，手动中止功能也会被禁用
3. 建议在测试环境中先验证修改的效果