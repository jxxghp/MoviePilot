# 2.2.0 数据库升级脚本修复说明

## 问题描述

在运行 2.2.0 版本的数据库升级脚本时，遇到以下错误：

```
表 downloadhistory 存在序列，需要修复
转换表 downloadhistory 序列时出错: (psycopg2.errors.ObjectNotInPrerequisiteState) column "id" of relation "downloadhistory" is already an identity column

[SQL: 
            ALTER TABLE "downloadhistory" 
            ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (START WITH 1605)
        ]
```

## 问题原因

1. **模型定义问题**: 在 `app/db/__init__.py` 的 `get_id_column()` 函数中，对于 PostgreSQL 数据库，已经使用了 `Identity(start=1, cycle=True)` 来定义 ID 列。

2. **检查逻辑缺陷**: 升级脚本的检查逻辑不够准确，没有正确识别已经是 IDENTITY 类型的列。

3. **错误处理不足**: 脚本没有针对 "already an identity column" 错误进行特殊处理。

## 修复方案

### 1. 改进 Identity 检查逻辑

**修改前**:
```python
# 只检查 column_default
if column_default and 'GENERATED BY DEFAULT AS IDENTITY' in column_default:
    print(f"表 {table_name} 的id列已经是Identity类型，跳过")
    return
```

**修改后**:
```python
# 使用 information_schema.columns 的 is_identity 字段
result = connection.execute(sa.text(f"""
    SELECT column_name, data_type, is_nullable, column_default, 
           is_identity, identity_generation, identity_start, identity_increment
    FROM information_schema.columns 
    WHERE table_name = '{table_name}' 
    AND column_name = 'id'
"""))

# 双重检查：is_identity 字段 + column_default
if is_identity == 'YES' or (column_default and 'GENERATED BY DEFAULT AS IDENTITY' in column_default):
    print(f"表 {table_name} 的id列已经是Identity类型，跳过")
    return
```

### 2. 添加双重检查机制

在 `convert_to_identity` 函数中添加转换前的再次检查：

```python
# 再次检查是否已经是Identity类型（双重检查）
result = connection.execute(sa.text(f"""
    SELECT is_identity
    FROM information_schema.columns 
    WHERE table_name = '{table_name}' 
    AND column_name = 'id'
"""))

is_identity = result.fetchone()
if is_identity and is_identity[0] == 'YES':
    print(f"表 {table_name} 的id列已经是Identity类型，跳过转换")
    return
```

### 3. 改进错误处理

添加特定错误处理，忽略 "already an identity column" 错误：

```python
except Exception as e:
    print(f"转换表 {table_name} 序列时出错: {e}")
    # 如果是已经存在的Identity错误，则忽略
    if "already an identity column" in str(e):
        print(f"表 {table_name} 的id列已经是Identity类型，忽略此错误")
        return
    raise
```

## 修复效果

1. **准确识别**: 能够正确识别已经是 IDENTITY 类型的列
2. **避免冲突**: 避免对已经是 IDENTITY 的列执行转换操作
3. **优雅处理**: 对于意外出现的 "already an identity column" 错误进行优雅处理
4. **保持功能**: 保持原有的序列转换功能不变

## 测试验证

修复后的脚本能够正确处理以下情况：

- ✅ 已经是 IDENTITY 类型的列：跳过转换
- ✅ 普通序列列：正常转换为 IDENTITY
- ✅ 没有默认值的列：正常转换为 IDENTITY
- ✅ 意外错误：优雅处理并继续执行

## 影响范围

此修复主要影响：
- `downloadhistory` 表
- `downloadfiles` 表
- 其他使用 `get_id_column()` 定义的表

修复后，这些表的升级将能够正常完成，不会再出现 "already an identity column" 错误。